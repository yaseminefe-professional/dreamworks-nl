# DreamWorks

Website project for DreamWorks.

## Structure

- `index.html` — main page
- `css/style.css` — styles
- `js/main.js` — scripts

## Development

Open `index.html` in a browser, or serve the folder with any static file server.
